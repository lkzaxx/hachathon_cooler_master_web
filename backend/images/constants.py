import json

# Define image generation parameters as a Python dictionary
IMAGE_GENERATION_PARAMS = {
    "front_material": "brushed aluminum",
    "features_block": "glowing purple RGB lighting",
    "side_frame": "aluminum trim",
    "angle": "front-left 3/4 view",
    "camera_height": "camera angle 45 degrees",
}

# If you specifically need it as a JSON string:
# IMAGE_GENERATION_PARAMS_JSON = json.dumps(IMAGE_GENERATION_PARAMS, indent=4)
